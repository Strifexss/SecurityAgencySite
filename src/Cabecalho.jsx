import styled from "styled-components"

const Main = styled.header`
    color: red;
`

export default function Cabecalho() {
    return(
        <header>
            <h1>Teste</h1>
        </header>
    )
}